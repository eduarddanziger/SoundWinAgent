<Window x:Class="SoundDefaultUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        Title="{Binding WindowTitle}" Height="320" Width="700"
        materialDesign:ColorZoneAssist.Mode="PrimaryLight"
        >
    <DockPanel Margin="10">
        <Grid DockPanel.Dock="Top">
            <StackPanel>
            <TextBlock Text="PnP Identifier" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Device.PnpId, Mode = OneWay, TargetNullValue='(No device)', FallbackValue='(No device)'}" IsReadOnly="True" Margin="0,0,0,10"/>

            <TextBlock Text="Name" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Device.DeviceName, Mode = OneWay, TargetNullValue='(No name)', FallbackValue='(No name)'}" IsReadOnly="True" Margin="0,0,0,10"/>

            <TextBlock Text="Output Volume Level" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Device.RenderVolumePercentText, Mode = OneWay, TargetNullValue='-', FallbackValue='-'}" IsReadOnly="True" Margin="0,0,0,10"/>

            <TextBlock Text="Input Volume Level" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Device.CaptureVolumePercentText, Mode = OneWay, TargetNullValue='-', FallbackValue='-'}" IsReadOnly="True" Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
        <Button Content="Refresh Volume Level"
                Command="{Binding RefreshCommand}"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Width="230"
                />
    </DockPanel>
</Window>
