<!-- ReSharper disable once Xaml.StaticResourceNotResolved -->
<Window x:Class="SoundDefaultUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:SoundDefaultUI"
        xmlns:controls="clr-namespace:SoundDefaultUI.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        Style="{StaticResource MaterialDesignWindow}"
        Title="{Binding WindowTitle}" Height="690" Width="750"
        materialDesign:ColorZoneAssist.Mode="{Binding Source={x:Static local:ThemeService.Instance}, Path=IsDarkTheme, Converter={StaticResource ThemeToColorZoneModeConverter}}">
    <Window.Resources>
        <local:ThemeToColorZoneModeConverter x:Key="ThemeToColorZoneModeConverter" />
        <Style x:Key="GrayOnDisabledTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="LightGray" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel Margin="5">
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0" />

        <StackPanel Orientation="Vertical" Margin="-10,0,0,0">
            <Expander Header="Default Render Device" IsExpanded="True">
                <controls:DefaultDeviceControl DataContext="{Binding RenderDevice}" />
            </Expander>
            <Expander Header="Default Capture Device" IsExpanded="True">
                <controls:DefaultDeviceControl DataContext="{Binding CaptureDevice}" />
            </Expander>
        </StackPanel>
    </DockPanel>
</Window>